org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# KSP2 is left off (default) — Room 2.6.1 + Hilt 2.52 are best-verified on
# the KSP1 implementation as of this writing, and the build sandbox that
# authored this repo has no way to actually compile-test the alternative.
