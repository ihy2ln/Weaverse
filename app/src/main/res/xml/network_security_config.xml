<?xml version="1.0" encoding="utf-8"?>
<!-- Allow cleartext HTTP for LAN Wi‑Fi sync (peer IPs / desktop host). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
